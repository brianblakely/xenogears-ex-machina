/goal Complete Phase 1 of Xenogears Ex Machina by extending the connected recovered program, not by accumulating isolated proofs. Read AGENTS.md, plan.md, docs/connected-decomp.md, docs/phase1-slice-contract.md and the current source/state handoff. Stay in Phase 1 until its actual exit is demonstrated; do not spawn broad agents for successive phases or start the later runtime/editor features.

Start with the existing xem-reconstruct target and connected tests. Reuse a source-qualified original case when available. Otherwise prepare one event-pass entry boundary from existing qualified captures before claiming original connected progress. Keep source/capture identities and comparison scope explicit; synthetic fixtures are not original execution.

Run the connected reconstruction and inspect its first encountered dependency or state divergence. For an instruction blocker, inspect analysis/recovery.json and existing source first: integrate an already recovered handler rather than re-decompiling it. For missing prepared input, qualify the real input. For missing integration, connect the existing modules and shared state. For genuinely missing game behavior, recover the smallest connected source boundary that resolves the blocker using Ghidra, callers/callees and selective assembly or m2c review.

Implement stable behavior directly in the shared C++ reconstruction library. Python remains extraction, orchestration and independent comparison tooling, not a mandatory gameplay-model stage. Test the same C++ implementation that the analysis driver and later native runtime use. Rerun from the immutable prepared starting state; expected observations are assertions, never injected intermediate results. Preserve existing synthetic and original comparisons and report the new executable boundary, first divergence or blocker, and remaining scope.

Advance the driver beyond its initial prepared event pass by connecting the recovered field-update tail and owned state; separately move entry backward through qualified initialization/loading. Never repeat isolated event passes as invented game ticks. Extend toward the existing frozen slice and required alternatives without shrinking its obligations. Unknown operations stop explicitly; no emulator fallback, guessed formula, silent opcode, fabricated readiness or debugger-only bypass may create progress.

Use one integration owner. Delegate only bounded source/capture questions with explicit shared-state contracts, then integrate the results into the same executable path. Run focused checks in the inner loop and comprehensive repository/provenance/distribution checks at meaningful milestones. Keep historical evidence and Phase 0 artifacts; do not expand administrative frameworks. Mark plan todos or the Phase 1 exit complete only when their existing evidence requirements are actually met.
