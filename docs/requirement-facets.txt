# Reviewed task | independently tracked facets | validation procedure and acceptance oracle.
# Source and facet identities are bound by docs/requirement-review.json.
P00-T01 | every requested feature; every platform; every default setting; every editor capability | Audit each plan task and table against explicit facets; validation must reject omissions and source drift. Test definitions and executed results remain separate.
P00-T02 | both US discs; reference selection; container hashes; decoded source hashes; region evidence; exact supported revisions; rejection of filename-only identification | Re-run container integrity, raw-track and boot-executable hashes; inspect original metadata and declare exact hash profiles. Unsupported or unproven revisions remain explicit.
P00-T03 | fields; world map; battles; menus; event sequences; audio; FMVs; saves; optional activities; minigames | Reconcile an original-source inventory for both discs with observed content IDs and provenance; unknown counts or unidentified content remain open and prevent an exhaustive claim.
P00-T04 | runtime source area; independent analysis area; extraction tools area; editors area; tests area; packaging area; ignored original discs; ignored generated local assets | Inspect repository ownership and source archive allowlist; attempt to include private images, stores and saves and require rejection.
P00-T05 | disc and revision; file or sector offset; executable identity; overlay identity; applicable address; observed behavior; confidence; reproducible validation procedure | Validate a real original-data finding and reject incomplete or unqualified evidence records; re-run its recorded measurement.
P00-T06 | identified state; analyzed state; decompiled state; natively implemented state; behaviorally validated state; unknown behavior; intentionally changed behavior | Validate subsystem records and gated transitions with evidence for every promoted state; reject skipped proof and changes without a decision record.
P00-T07 | Arch Linux environment; C++ build; CMake; Ninja; Debug configuration; sanitizers; formatting; automated tests; reproducible empty-project build | Run locked Nix setup, clean Debug/ASan+UBSan/Release builds, CTest, formatting and repository checks; compare clean release artifacts and inspect installation on the declared Arch host.
P00-T08 | contributor provenance; dependency review; independent implementation boundary | Review contribution/dependency templates and actual dependency inventory; require source origins, license, scope and review, and forbid other Xenogears implementations or RE foundations.
P00-T09 | representative scenarios; disc 1 checkpoints; disc 2 checkpoints; cross-disc progression; definitions before implementation | Audit stimulus, prerequisites, original oracle, expected state surfaces and provenance requirements for each checkpoint; distinguish proposed checkpoints from captured reference runs.
P00-T10 | general testing design informed by sibling framework; checkpoint-free cold boot; original loader entry; declarative setup through verified scene adapters; explicit readiness conditions; bounded timeouts; ordered inputs; original-source identity; tool and write provenance; unrecovered battle adapters tracked and rejected; unrecovered position adapters tracked and rejected; unrecovered progression adapters tracked and rejected | Run supported declarative original-program scene scenarios from cold boot without checkpoint prerequisites; verify original loader/readiness evidence, controlled setup and ordered inputs on supported profiles. Readiness must name the actual observed condition, such as map initialized, without claiming player control. Reject incompatible source fingerprints, known missing data and unsafe/unresolved adapters; demonstrate timeout failure instead of claiming readiness. Preserve hashes and exact setup writes in local evidence and retain the explicit battle/position/progression backlog. Sibling game-specific code/offsets/catalogs are not Xenogears evidence.
P00-T11 | agent-native execution coverage; full introspection coverage; direct commands coverage; complete debug and go-anywhere coverage; optional image-stream coverage; unlocked-speed coverage; optional PS1 visuals coverage; Wide headphone surround coverage; preserved historical evidence; unimplemented additions remain defined | Review every changed task, boundary, foundational-contract row, default, platform and gate against the migrated matrix; reject source drift and evidence reassignment. Check the explicit migration record and prove later native requirements have no inherited passes.
P00-T12 | versioned native protocol; structured state-query schema; typed command model; explicit simulation-time units; execution modes; debug permission boundary; device-free headless architecture; first-slice acceptance gates | Review docs/agent/README.md, machine-readable schemas, methods, examples and acceptance gates together. Validate request/result shapes and negative cases; audit tick ordering, state consistency, cancellation, ownership, debugging and renderer independence. These are specification tests, not a native gameplay pass.
P00-T13 | scenario contract inventory; schema inventory; runner and executor inventory; versioned parity table; evidence-qualified emulator capabilities; explicit emulator adapter backlog; mapped native equivalents; mapped native extensions; parity drift detection | Inspect the three named baseline sources and their transitive executor/sampling/trace behavior. Bind parity rows to source hashes, tests and original findings; compile presets and reject unsupported setup. Keep observed emulator paths, synthetic tooling checks and unimplemented native capabilities distinct.
P00-T14 | foundational agent-authoring contract coverage; selected TypeScript and optional TSX stack; separate type-check and transpile stages; geometry and asset adapter responsibility review; source build and native runtime boundaries; untrusted generator and installation policy; early source-to-playable and expanded acceptance gates; separately pinned and reviewed authoring dependencies; preserved historical evidence without inherited authoring passes | Review every new or changed task, authoring-contract and selected-stack row, project boundary, default, phase exit and release checkpoint against explicit facets. Audit dependency pins, licenses and build/runtime scope separately. Run parser, drift, migration and gate rejection tests; all native authoring facets remain defined until actual-runtime evidence exists.
P01-T01 | disc layout; boot metadata; executable loading; archives; sector references; compression; shared resources; disc-specific resources | Reproduce offsets and decoding from both original profiles; round-trip supported formats and explicitly classify unsupported entries; every claim links to independently collected evidence.
P01-T02 | main executable map; dynamically loaded code; dynamically loaded data; overlay-qualified addresses; reused memory identities | Trace loader transitions and show two overlays at a reused address retain distinct identities and symbols; compare map to original bytes and execution.
P01-T03 | MIPS disassembly; function boundaries; calling conventions; globals; structures; tables; dispatchers; subsystem relationships | Review original disassembly and call/data references; focused original traces and boundary cases must support each recovered symbol and relationship.
P01-T04 | readable decompiled source; independent review; inferred types; control flow; pointer arithmetic; signedness; fixed-point arithmetic; overflow behavior | Compare reviewed reconstruction against original traces at normal and arithmetic boundary inputs; document defined native arithmetic and unresolved cases instead of accepting automatic pseudocode.
P01-T05 | hardware-facing code; BIOS interfaces; GPU command generation; audio interfaces; controller reads; disc I/O; gameplay intent separated from PS1 mechanisms | Trace the original call sites and dependencies for each named hardware-facing facet used by the reviewed first playable slice; document input/output, state, ordering and timing intent plus the native service boundary. Validate the recovered intent against source-qualified original execution. Later native platform facets require actual native service execution; Phase 1 analysis does not claim it.
P01-T06 | projected-vertex precision; texture interpolation; color quantization; dithering; rasterization resolution; primitive ordering; transparency; original visual references | Recover each operation from original source and matched captures with exact profiles and coordinates. Document applicable render stage, scene, parameters and tolerances; no full-screen approximation or synthetic image establishes original fidelity.
P01-T07 | Mono signal; Stereo signal; Wide signal; panning and polarity; reverb; master output; streamed and FMV routing; matched raw two-channel captures; intended playback model; matrix versus phase-expansion discrimination | Capture identical original scenarios and levels in each sound mode on both discs; trace signal operations and routing, compare phase/polarity/timing and primary documentation where available. Keep ambiguities open and defer decoder selection until the spatial reference is supported.
P01-T08 | original execution tracing; captured inputs; seeds; state transitions; script instructions; timing observations | Re-run exact original profile with documented emulator/hardware setup and input log; corroborate state and timing observations, recording confidence and divergence.
P01-T09 | field formats; geometry formats; sprite formats; animation formats; collision formats; camera formats; texture formats; palette formats; UI formats; dialogue formats; battle formats; world-map formats; audio formats; FMV formats | Derive format fields from original bytes and use bounded local parsing fixtures plus independent behavioral evidence; preserve unknown fields and reject truncation/corruption.
P01-T10 | event bytecode; operands; scheduling; waits; branching; concurrent actors; side effects; original disassembler; instruction reference | Decode and trace each used instruction family, branch and concurrent schedule; compare effects to original and report unsupported opcodes with source/PC rather than no-op.
P01-T11 | movement rules; jump rules; encounter logic; RNG; battle formulas; enemy behavior; story-state changes; minigame rules; save formats | Capture original normal, boundary and failure cases with controlled inputs/seeds; validate reconstructed outcomes and arithmetic against those observations.
P01-T12 | simulation cadences; frame counters; asynchronous load dependencies; interrupts; hardware timing; gameplay timing rules; implementation timing artifacts | Vary observed original load/timing conditions and measure subsystem changes; classify evidence-supported gameplay cadence separately from presentation or hardware delay.
P01-T13 | state ownership; action eligibility; menu and dialogue structure; event and control readiness; spawn preconditions; transition preconditions; subsystem initialization; semantic field correlations independent of memory addresses | Trace ordinary entry and action acceptance/rejection in the selected original slice, including internal and offscreen state. Record semantic IDs, valid setup and stable readiness with original coordinates; require these proof obligations at the slice gate.
P01-T14 | original parser fixtures; per-finding behavioral tests; local original-data fixture generation; public synthetic fixtures; redistributable CI fixtures | Public CI runs without copyrighted data; local tests bind to source profile and independent expected results; fixture manifests disclose origin and cannot promote synthetic results to behavioral proof.
P01-T15 | unresolved symbols; unknown formats; unimplemented instructions; explicit unknown behavior errors | Inject an unresolved case in each inventory and require precise diagnostic and blocked affected coverage, with no silent no-op or progression bypass.
P02-T01 | simulation interface; original-script interface; rendering interface; input/UI interface; audio/media interface; asset-management interface; persistence interface; editor-services interface; agent and control service interface; one simulation for humans agents tests and editors | Build interface-level tests with replaceable adapters; verify authoritative state has no platform-object dependency and headless execution needs no renderer. Verify all clients share the actual authoritative engine, loaders and command handlers.
P02-T02 | independent extractors; independent decoders; bounded reads; corruption checks; source hashes; versioned caches; unsupported-revision errors | Import local verified source profiles and compare independent findings; mutate/truncate assets and stale cache versions; reject invalid input without out-of-bounds access or partial trusted cache entries.
P02-T03 | stable imported IDs; address-independent IDs; resource provenance; unknown source data preservation | Reimport after relocation and verify stable IDs and source-offset links; round-trip untouched unknown fields and detect source/version mismatch.
P02-T04 | SDL3 windowing; SDL3 platform integration; SDL3 input; initial Arch Vulkan renderer; optional window initialization; optional GPU initialization; optional audio-device initialization; pure headless path initializes none | Launch a native SDL3 window on Arch, render a reviewed backend scene and receive device events; record dependency provenance and host/driver. Run logical gameplay with those services unavailable and audit initialization/dependencies.
P02-T05 | rendering abstraction; shader pipeline; Linux Vulkan; Windows Direct3D 12; macOS Metal; SDL3 GPU evaluation; small original backend validation programs | Compile/run minimal independently authored backend probes on each declared target; compare shader behavior and document SDL3 GPU decision and limits.
P02-T06 | Modern render stages; selectable PS1 geometry operations; selectable rasterization operations; selectable compositing operations; visual precision excluded from authoritative state | Toggle each independently staged operation over one recorded trace; compare authoritative hashes and inspect stage-specific original fidelity fixtures.
P02-T07 | original sound generator boundary; raw two-channel Wide tap; Wide-specific reconstruction; HRTF stage; phase and timing preservation; original-mode bypass; null output media progression | Inspect signal taps and bypass routing with phase/polarity fixtures and verified original captures; disable output and require identical logical media/script events. Gate reconstruction on Phase 1 Wide findings.
P02-T08 | native resource management; typed state; explicit native services; no PS1 memory-layout dependence; no original BIOS execution; no PS1 CPU loop | Inspect executable dependencies and run isolated native subsystem tests with no BIOS/PS1 execution support installed; verify typed resource lifetime under sanitizers.
P02-T09 | independent simulation clock; independent render clock; recovered gameplay arithmetic; recovered subsystem updates; high-precision presentation transforms; independent wall clock | Replay a fixed input/seed scenario over varying render cadence; compare canonical simulation state while checking smooth high-precision presentation. Compare stepping and unlocked execution without wall-time-derived gameplay updates.
P02-T10 | entity state; party state; inventory state; story flags; RNG state; event state; battle state; minigame state; logical audio positions; media timelines | Mutate each authoritative domain, serialize/restore and compare canonical state; no domain can be silently omitted or reconstructed from nondeterministic presentation state.
P02-T11 | serializable stable IDs; serializable handles; rebuildable GPU objects; rebuildable decoder internals; rebuildable caches; rebuildable platform handles | Restore into a fresh process with all transient services recreated; validate every handle and canonical state and reject dangling references.
P02-T12 | deterministic original-script scheduling; deterministic Lua task scheduling; persistence contract; explicit task progress; no arbitrary Lua stack serialization assumption | Suspend concurrent tasks at documented boundaries, restore and replay; compare scheduling and effects and reject unsupported persistence patterns with actionable errors.
P02-T13 | asynchronous asset availability; background-job order; deterministic simulation outcomes; deterministic event ordering; explicit required-data gates | Inject randomized job delays and completion orders into repeated seeded replays; authoritative hashes and events must remain equal or wait at an explicit deterministic boundary. Record deterministic load gates and verify host completion order cannot change event order.
P02-T14 | shared native content interface; shared entity interface; shared collision interface; shared event interface; original and authored package inputs; separate origin-specific decoding; recovered movement rules for generated content; single gameplay model | Load equivalent original-derived and authored fixtures through their origin adapters; verify both reach the same entity, collision, movement and event services. Reject any path that bypasses recovered movement eligibility or supplies a second simulation; compare authoritative state and command behavior.
P02-T15 | versioned API; command-line client; local process I/O or IPC; request and response IDs; schema discovery; capability discovery; stable IDs; structured errors; no GUI account or agent-vendor dependency | Run the protocol acceptance gates with a plain pipe client from a clean device-free environment. Exercise negotiation, discovery, malformed messages, bounded responses and unsupported methods; preserve transcripts and exact build/schema identities.
P02-T16 | directional and analog input; run; jump; confirm and cancel; camera rotation; menu toggles; semantic item selection; scrolling; dialogue choices; battle commands and targets; minigame controls; direct pointer UI tests | Invoke each action through typed engine commands in an eligible state and a disabled state; compare physical-client handlers and resulting state. Pointer tests target native UI coordinates and need no OS events.
P02-T17 | same human and agent command handlers; same eligibility checks; same transitions; no OS event injection; no synthetic SDL events; no virtual devices; no screenshot OCR or desktop focus dependency | Replay identical canonical actions from human and agent clients; compare acceptance, applied ticks and state hashes. Inspect dependency and event paths and run without window/input services.
P02-T18 | press hold release semantics; simultaneous actions; analog ranges; exact tick scheduling; ordered batches; applied-tick acknowledgments; invalid-action reasons; no unintended partial mutation | Exercise boundary ticks, simultaneous axes/buttons, conflicts, delayed/stale batches and invalid final actions. Require deterministic order, release behavior and atomic validation with unchanged state on rejection.
P02-T19 | stable menu selection IDs; dialogue choice IDs; battle target IDs; enabled and disabled actions; disabled reasons; focus; scrolling state; control ownership; rule-preserving semantic selection | Query each decision surface and attempt every enabled/disabled selection, including stale IDs. Compare legal human selection; no semantic selection bypasses locks, resource costs or progression.
P02-T20 | active and inactive entities; components and transforms; collision and terrain; camera; maps and transitions; flags and variables; party stats equipment and inventory; encounters; battle and AI; minigames; original script stacks PCs and waits; Lua tasks; RNG state; pending events and timers; logical audio and media; internal and offscreen state | Inventory every serialized authoritative field and match it to a typed discoverable query path; mutate each domain in valid native scenarios and compare full snapshot, query and restored hashes. Omission of implemented state fails the subsystem gate.
P02-T21 | subsystem diagnostics; resource and loading status; render status; audio status; native symbol and source correlations; diagnostics outside authoritative state; schema coverage gate | Introduce a new state member and require the registration gate to fail until query/serialization coverage exists. Inspect source correlations and prove changing diagnostics does not alter authoritative hashes.
P02-T22 | consistent tick-tagged snapshots; filtered queries; state diffs; subscriptions; event streams; state hashes; snapshot-consistent pagination; explicit truncation | Query and paginate while advancing and restoring; pages must retain one immutable snapshot, deltas require matching bases, gaps/expiry are explicit, and bounded results never silently omit state.
P02-T23 | atomic observe-act-advance-observe; bounded run-until; identified-state preconditions; service responsive while paused; service responsive while running; cancellation; input ownership and handoff | Race stale observations and competing controllers against a bounded cycle. Verify one owner, atomic initial admission, responsive cancellation/queries, exact stop boundaries and no ambiguous applied command.
P02-T24 | normal actions distinct from debug; opt-in debug mutation; setup and snapshot provenance; debug-assisted outcome classification | Attempt mutation as a gameplay client, then with explicit debug authority; require denial or labeled audit events. Completion evidence must distinguish typed initial setup, later debug corrections and normal play.
P02-T25 | privileged control local by default; read-only spectators; explicit trusted debug; authenticated opt-in remote control; resource bounds; cancellation at unlocked speed | Exercise role matrix including untrusted Lua, spectators and competing local controllers. Remote access requires explicit enablement/authentication; floods and unthrottled execution cannot suppress bounded control/cancellation.
P02-T26 | real-time mode; fixed multiplier; paused mode; exact stepped mode; fully unthrottled mode; no artificial unlocked sleep or wall cap | Run identical seeded traces in each mode; compare every tick hash and stop reason, inspect pacing waits and record actual ticks/second. Unlocked execution must be independent of a fixed acceleration preset.
P02-T27 | exact tick count; typed condition stop; event stop; decision-point stop; breakpoint stop; simulation budget; stop reasons; simulation elapsed time; wall time and throughput; safe interruption | Exercise success, budget, breakpoint and cancellation at boundary ticks, including zero-work satisfied conditions. Check exact applied work and consistent resumable state; wall measurements are diagnostics.
P02-T28 | unchanged recovered tick semantics; every gameplay update; collision work retained; event work retained; no automatic cutscene skipping; logical media completion equivalence | Compare per-tick hashes and update counters at real-time and unlocked speed during movement, battle and concurrent media/events. Reject widened timesteps, dropped updates and manufactured completion shortcuts.
P02-T29 | pacing independent of VSync; GPU independent clock; audio-clock independence; video-playback independence; encoder independence; spectator independence; null and rebuildable presentation; measured throughput | Stall each output service and run the same bounded trace without it. Require equal authoritative outcomes and progressing simulation; record computation cost without promising a fixed hardware-independent multiplier.
P02-T30 | portable exact debug snapshots; restore; reset; seeded replay; branching test sessions; available before first slice | Restore into a fresh process and branch at field/event/battle boundaries; compare all state and subsequent replay ticks. Reject incompatible schema/content/mod identities and isolate persistence per branch.
P02-T31 | pause and resume; simulation step; original-script task and instruction step; Lua task and instruction step; conditional breakpoints; state watchpoints; event and transition breakpoints; variable inspection and editing; call and task stacks; traces; native debug symbols; native debugger integration; memory assertion crash and hang inspection | Demonstrate each stop/inspection mode with source correlations and continuation semantics. Capture a controlled assertion/hang, obtain native stack/memory through the documented debugger path, and replay the bounded failure history.
P02-T32 | typed guarded mutation; state validation; preconditions; rollback; before and after diffs; mutation provenance; unsafe raw diagnostics restricted | Submit valid and invalid multi-field edits with stale guards; invalid edits leave exact prior state. Capture auditable diffs and require separate unsafe authority for raw diagnostics, never as a gameplay requirement.
P02-T33 | structured logs; coverage; event histories; performance counters and profiles; assertion and crash artifacts; stacks and state dumps; replay inputs; bounded recent history | Trigger deterministic assertion, malformed input and injected hang cases; require terminal failure manifests with source/build/mod/schema identities, stop state and replay commands. Verify ring-buffer bounds and explicitly report missing artifacts after abrupt death.
P02-T34 | cold reset without saves; source and catalog validation; numeric and named entry; ordered input wait and capture; readiness predicates; bounded execution; guarded setup; success and failure artifacts; exact provenance | Execute every applicable row in docs/agent/emulator-parity.json against native services; compare intended behavior to evidence-qualified original scenarios. Native ticks and IDs require explicit semantic mapping, never copied RAM hooks.
P02-T35 | emulator contract used as baseline; independently implemented native services; verified Kernel scope; verified numeric-field scope; position backlog explicit; party and progression backlog explicit; battle and other entry backlog explicit | Review parity evidence scopes and run positive/negative adapter cases. No backlog row may be labeled verified without new original evidence, and native coverage needs separate execution results.
P02-T36 | field and map entry; world-map location; battle and formation; minigame; cutscene and event; menu entry; both discs; player and party position and facing; collision-safe spawn; party stats equipment inventory; story and progression; encounter setup; camera setup; RNG seed; mode and configuration | Cold-create valid and invalid scenarios for every implemented entry family via normal native initialization. Require schema/catalog discovery, complete dependent state and actionable readiness with no old save prerequisite.
P02-T37 | shared ordinary loaders; shared initialization; shared validators; dependent state reconstruction; invalid combination rejection; unknown entry errors; atomic setup; no hidden save prerequisite | Compare ordinary transitions with typed setup; inject bad resources, colliding spawns and contradictory progression. A failed preparation cannot replace or partially modify the live session.
P02-T38 | asset availability; map initialization; script completion; actionable dialogue and menu; battle readiness; actual player control; recovered logical ticks; no implicit emulator-frame conversion | Stop separately at each semantic readiness condition in original-correlated native scenarios; prove initialized but script-owned scenes do not satisfy player control. Validate cadence identity and reject unqualified frame-to-tick imports.
P02-T39 | discoverable location event formation catalog; declarative conditions; setup; actions; waits; assertions; snapshots; optional images; maintained parity table | Discover content and compile/run a scenario from schemas alone. Version and regenerate parity checks whenever either apparatus changes; unsupported IDs and missing capabilities fail explicitly.
P02-T40 | source asset build and mod identities; normalized setup; seeds; ordered commands; applied ticks; guarded mutations; expected and observed readiness; hashes; captures; terminal status; exploratory setup labels | Recreate a run from its manifest and compare state/event trace. Reject missing provenance and distinguish exploratory/unsafe setup from validated normal gameplay evidence.
P02-T41 | simulation-work budget; independent wall watchdog; no inherited emulator frame ceiling; readiness without real-time sleep | Bound a nonterminating condition by ticks/work and an injected deadlock by wall watchdog; require failure artifacts and an exact stop state when available. Test native budgets beyond 36000 without changing tick semantics.
P02-T42 | image-free default; on-demand screenshot; optional same-session offscreen stream; no GPU in logical mode; explicit image renderer requirements | Launch with display/input/audio/GPU access absent, play and query state, then enable images on renderer-capable hardware. A unavailable renderer returns a presentation error while simulation stays usable.
P02-T43 | spectator client documentation; attach without restart; detach without restart; read-only observation; explicit control transfer; no viewer prerequisite | Attach, disconnect and reconnect a viewer during agent play; verify session continuity and equal trace hashes. Spectator mutation fails until deliberate role/ownership transfer.
P02-T44 | image session ID; tick and logical time; render profile; dimensions; configurable resolution; configurable cadence; paused image without stepping | Inspect image envelopes at varied cadence/resolution and while paused; metadata must identify the rendered immutable state and no screenshot request advances authoritative time.
P02-T45 | bounded rendering queue; bounded encoding queue; bounded transport queue; sample and drop policy; no viewer backpressure; drop telemetry; honest accelerated timestamps; explicit offline exact-capture cost | Throttle or stop consumers and exhaust queues while running unlocked. Old images must drop and report counts while simulation progresses; exact offline capture is explicit and separate from live pacing.
P02-T46 | same action trace with images absent; offscreen equivalence; streamed equivalence; state-hash equivalence; no outcome or clock dependence on observation; full introspection without images | Run identical complete slice traces in all presentation modes, including slow/disconnected viewers. Compare all authoritative hashes and report performance separately from correctness.
P02-T47 | early Windows compile check; early macOS compile check; Arch execution priority; Arch integration priority; Windows headless protocol check; macOS headless protocol check | Compile on declared Windows/macOS targets and run Arch integration; record target/compiler versions and do not claim runtime validation from compilation. Run the same protocol conformance client natively on each platform; compile-only results do not prove headless operation.
P02A-T01 | typed TypeScript authoring SDK; distinct solid geometry and open surfaces; distinct render mesh and collision definitions; typed asset references; typed entities and runtime events; ordinary function SDK first; optional minimal TSX factory after function parity; serializable definitions without React lifecycle; no implicit per-frame generation | Type-check positive and invalid SDK examples. Build identical content using ordinary functions first, then optional TSX and compare normalized outputs. Require geometry/domain type errors, serializable definitions, and no rendering or UI lifecycle dependency.
P02A-T02 | versioned intermediate representation; namespaced content IDs; stable entity instances; reusable mesh references; transforms and materials; explicit collision; spawns portals and triggers; NPCs and encounters; event graphs; identity independent of paths names triangles and PS1 addresses; unflattened semantic world objects | Round-trip a multi-instance world IR, rename files/display labels and retessellate meshes without changing content identities. Validate references, version handling and separate collision/event records; ensure individual town objects remain queryable.
P02A-T03 | authoritative TS and TSX source; authoritative parameter files; authoritative Lua and source assets; generated manifests only build products; graphical exposed-parameter edits; reviewable source patches; preservation of arbitrary handwritten code | Edit declared parameters and apply a reviewed source patch through graphical and agent clients. Rebuild identical packages through one compiler, retain unrelated handwritten code, and reject edits that treat generated manifests as an independent source of truth.
P02A-T04 | separate tsc no-emit type checking; separate esbuild transpilation and bundling; isolated pinned Node execution; dependency and reference resolution; validation stage; export stage; structured stage errors; unsupported callbacks rejected; nonserializable state rejected; invalid references rejected; nonfinite geometry rejected | Inject one failure into each distinct compiler/build stage and require the precise structured stage/code/source diagnostic, nonzero failure and no successful package publication. Successful builds must execute source and manufacture native-loadable assets after type-checking and transpilation.
P02A-T05 | project-owned normalized mesh data; positions and indices; normals and UVs; material groups and bounds; source and object attribution; winding validation; degeneracy validation; topology requirements; resource budgets; attribute consistency; reliable geometry-kernel allocation release; measured optional meshoptimizer use | Normalize Manifold solids and surface/custom/import adapters into the same mesh schema. Exercise invalid attributes, winding, topology and budget cases; repeat failed and successful builds to verify allocation cleanup. Any mesh optimization requires recorded benefit and preserved collision/material/source semantics.
P02A-T06 | GLB render assets via glTF-Transform core; versioned world manifests; separate collision and event manifests; package dependencies; source maps; asset hashes; engine and schema compatibility; required local base-asset references; profiled custom packing decision; glTF excluded as gameplay or collision contract | Export a package using the supported glTF material/extension subset and inspect all manifest and provenance fields. Resolve its references with the native loader; reject missing or unsupported versions and demonstrate collision/events remain explicit even if the render GLB is replaced.
P02A-T07 | cgltf behind project C++ adapters; package parse and validation; required asset decoding; staged entities and components; collision structure construction; reference resolution; initialized event tasks; atomic field publication; explicit unsupported material and extension errors; missing and corrupt resource rejection; no partially loaded scene | Load a valid package through the actual C++ bridge, then inject each unsupported/corrupt/missing resource failure. Verify exact pre-load authoritative state survives every failure and only fully resolved scenes publish; parsing alone does not prove decoding, materials, GPU upload or gameplay integration.
P02A-T08 | same immutable package in logical and rendered modes; zero GPU allocation in logical mode; native GPU meshes and materials when enabled; origin-specific asset decoding; shared original and authored entities; shared movement and collision; shared interaction and event services | Play the same compiled package with GPU/display access absent and with the native renderer. Compare legal command traces and authoritative hashes; audit original-data and authored paths for the same movement/collision/event implementation.
P02A-T09 | immutable original asset store; separate mod layers; explicit dependencies; explicit overrides; locally resolved verified original IDs; locally verified original hashes; no extracted original assets in distributed sources or packages | Build generated-only and mixed-asset variants, prove the imported store is unchanged, and inspect the distributed source/package allowlist. Mixed packages reference locally supplied assets by verified ID/hash; wrong or missing originals fail explicitly without embedding private bytes.
P02A-T10 | reproducible source inputs; exact lockfiles; explicit seeds; generator and compiler versions; imported-asset hashes; relevant settings; output content hashes; immutable tested artifact; measured supported-platform determinism | Rebuild clean copies on every declared supported toolchain with identical recorded inputs and compare output hashes. Change each dependency/input and verify build identity invalidation. Pin runtime tests to the measured package; record unsupported cross-platform guarantees rather than relying on seeds alone.
P02A-T11 | complete dependency and parameter cache keys; reusable instancing; rebuild only affected components; build-time static generation; native door state and transform updates; no runtime boolean recomputation for ordinary interactions; separately specified runtime generator persistence | Change one component parameter and verify only dependent outputs rebuild while shared mesh instances survive. Trace rendered frames and door actions to prove static generators do not rerun. Reject runtime generation declarations lacking an explicit algorithm/state/persistence contract.
P02A-T12 | generated box primitives; generated cylinder primitives; solid union; solid difference; solid intersection; profile extrusion; revolution; arbitrary vertex and index meshes; imported-model path; actual vertices and triangles beyond prefabs | Build and inspect one nontrivial example of every initial operation, including a boolean doorway and a custom indexed mesh. Verify source creates mesh vertices/triangles and native-loadable content; prepared prefab placement alone cannot pass.
P02A-T13 | separate manifold-solid path; separate open-surface path; separate custom-mesh path; separate imported-model path; boolean operand validation; useful failure details; no forced solid conversion of arbitrary imports | Submit open, nonmanifold and invalid boolean operands alongside valid solids and external meshes. Require appropriate adapter selection, explicit topology errors with operand identity, and lossless supported non-solid paths without silent kernel coercion.
P02A-T14 | parameterized rooms; corridors; houses; stairs; bridges; doors; machinery; user-authored custom components; lower-level algorithm access; arbitrary mesh-array access | Build a parameterized component suite and a new custom object not present in the catalog. Vary dimensions and connectivity, inspect generated topology and collision, and demonstrate a user can supply algorithms and mesh arrays without engine C++ changes.
P02A-T15 | demonstrated path sweeps; demonstrated terrain and heightfields; use-case-driven specialized surfaces; discoverable unsupported lofting; discoverable unsupported deformation; discoverable unsupported rigging; no advertised unimplemented modeling operations | Exercise every advertised surface generator with source and native-loadable results. Query the capability catalog for unimplemented operations and require unsupported responses; add specialized algorithms only with a documented use case and acceptance example.
P02A-T16 | documented authoring units and coordinates; imported-asset conversions; geometry-backend conversions; renderer conversions; recovered collision arithmetic conversions; scale validation; handedness validation; normals validation; winding validation; precision boundary tests | Transform asymmetric measured fixtures across all coordinate systems and back. Compare scale, orientation, normals and winding, then traverse native collision at precision limits using recovered arithmetic; implicit or double conversions fail.
P02A-T17 | renderer-supported materials; texture density and projection; generated UVs; hard and smooth edges; vertex colors; semantic surface regions; material identity on boolean cuts; material preservation through parameter changes; explicit unsupported shader features | Render generated surfaces and cut faces with diagnostic materials in the native renderer, then change topology and parameters. Check UV density, edge normals, colors and region/material identity; unsupported material/extension features produce diagnostics before export or load.
P02A-T18 | static surface and triangle collision mode; composed primitive collision mode; separate simplified collision geometry; explicit no-collision mode; compilation to native movement representation; preserved openings and arches; no bounding-box or convex-hull doorway substitute | Traverse generated stairs, narrow/arched doors and obstructed passages through legal native movement for each supported collision mode. Verify clearance and blocked movement against actual colliders; compare deliberately wrong bounding-box/hull colliders to ensure acceptance rejects sealed openings.
P02A-T19 | named anchors; doorway connections; placement constraints; facing constraints; collision-aware clearance; room adjacency; repeatable prop distribution; persisted solved transforms and geometry; no runtime prompt interpretation; no traversal-test teleportation | Solve and build a connected layout twice with recorded inputs, inspect persistent transforms, then complete traversal through native commands. Invalid connections/clearance must fail with source-linked errors; tests must detect teleports or runtime prompt-dependent layout changes.
P02A-T20 | persistent component IDs; instance IDs; operation IDs; source spans in geometry and diagnostics; invalid-dimension attribution; boolean-operand attribution; blocked-interaction attribution; connectivity attribution; editable parameter mapping; semantic regions independent of triangle numbering | Introduce each geometric and gameplay failure, retessellate the mesh, and verify diagnostics still resolve to the same editable source component/operation/parameter. Triangle renumbering must not change semantic region identity.
P02A-T21 | separate build validity result; separate native traversal and collision result; separate presentation quality result; actual-runtime legal movement tests; optional native captures and overlays; no export or external-preview gameplay pass | Produce a valid GLB with a deliberately blocked doorway and require build success but native traversal failure. Evaluate presentation separately using native captures when enabled, preserving all three result categories and the failed gameplay assertion.
P02A-T22 | serializable event and condition definitions; interaction door-state and sound sequence; no retained JavaScript closures; instruction signature validation; target validation; state ownership validation; runtime capability validation; no embedded gameplay JavaScript or authored TypeScript-to-C++ translation | Compile declarative interactions to native instructions, reject closures and invalid signatures/targets/ownership, then run the package without Node. Compare event ordering and resulting state to the declared native instructions; unsupported calls must fail compilation.
P02A-T23 | native trigger instructions; door instructions; dialogue instructions; pickup instructions; condition instructions; ordered tasks; simulation-tick waits; incremental quest and cutscene expansion; recovered encounter battle and minigame expansion; discoverable unsupported operations | Run each implemented event instruction in the actual scheduler with stepped and unthrottled commands. Query the supported vocabulary and reject future operations until their recovered subsystem and source-to-runtime tests exist.
P02A-T24 | authoritative event-graph progress; wait conditions; explicit targets; timeouts; cancellation state; mod persistent data; source-correlated inspection; stepping and breakpoints; snapshot and replay support; no quest state in build JavaScript or closures | Pause an authored event at waits, branch/restore snapshots, cancel and replay it, and inspect all progress through native APIs with source mapping. Outcomes and resumable state must match with the authoring process absent.
P02A-T25 | packaged Lua module references; shared Phase 7 deterministic API; shared Lua permission model; ordinary authoring without C++ rebuild; explicit unsupported engine-capability boundary | Package a declarative event plus Lua extension and run with Node absent using deterministic native APIs. Verify permissions/persistence and unchanged engine binary; requests for a new engine operation must report unsupported instead of fabricating an SDK call.
P02A-T26 | movement primitives; one-time reward primitives; interaction primitives; quest-transition primitives; defined failure behavior; defined re-entry behavior; actor-arrival waits; simulation scheduler timers; independence from GUI animation and wall time | Exercise blocked arrival, repeated interaction, failed quest conditions and leave/re-entry around saves/rewind. Compare stepped and unthrottled execution for exactly-once rewards, bounded waits and equal state with GUI/rendering disabled.
P02A-T27 | published SDK types; serializable Zod and JSON Schema subset; native-validator schema parity; runnable original examples; capability and version discovery; compact agent guidance; exact catalog IDs; tags dimensions and anchors; animations and compatibility; references and provenance; optional previews; rebuildable SQLite FTS5 catalog when adopted | Discover and build content using only published SDK/catalog information. Compare Zod/exported JSON Schema/native validation on positive and negative fixtures; query exact IDs and unsupported states. If adopted, rebuild SQLite/FTS5 metadata and verify equivalent search results without making it an early bridge prerequisite.
P02A-T28 | verified original-content classification; partially understood classification; opaque preserved-only classification; unsupported classification; lossless unknown script blocks; factual game records separate from generated suggestions; resource existence distinct from safe editability | Query assets from each evidence class and attempt supported and unsupported edits. Unknown script bytes round-trip unchanged, opaque records reject semantic edits, and generated creative suggestions cannot acquire verified factual provenance.
P02A-T29 | headless content CLI; versioned JSON-RPC over local stdio or IPC; discovery operations; source and parameter edits; validation and build; native scenario launch; inspection traces and captures; tests diffs and packaging; optional MCP adapter; no chatbot network GUI or vendor prerequisite | Drive the complete discover/edit/build/play/inspect/test/package loop over local machine-readable requests with a generic client. Validate version/error/cancellation contracts and demonstrate every content-changing graphical operation has an equivalent headless service.
P02A-T30 | semantic edit commands; batched atomic transactions; expected source revisions; idempotent retries; dry runs; diffs; undo; cross-file validation as a unit; stale-write rejection; shared graphical command contracts; same pipeline for direct file edits | Submit concurrent stale and valid cross-file changes, repeat requests, dry-run and undo them. Failed or stale edits leave all files unchanged; successful agent, graphical and direct-file changes pass the same build and validation pipeline without losing unrelated work.
P02A-T31 | distinct source revision identity; distinct immutable package hash; distinct runtime session ID; distinct snapshot ID; cancellation; bounded queries; pagination; active tests pinned to immutable package revisions | Edit source and publish a later build while a scenario is running. Confirm the active session and snapshots retain the original package hash, paginated queries remain consistent, and cancellation is bounded without conflating any of the four identities.
P02A-T32 | machine-readable diagnostic codes; severity; source spans; object and operation IDs; observed and expected values; exact reproduction scenarios; execution traces; native state; optional object-ID overlays; collision path and camera overlays; editable-component correlation | Cause geometric, load and runtime interaction failures and require complete structured diagnostics with a reproducible scenario. When captures are enabled, resolve a selected/visible object through source maps to the same failing component and editable parameter.
P02A-T33 | executable generator and dependency-install threat model; OS or process isolation; filesystem restrictions; network restrictions; credential restrictions; controlled dependencies; separate output and temporary directories; memory and work limits; independent watchdog; Node vm and typing not isolation; no runtime debug authority for generators or Lua | Run malicious authored fixtures attempting out-of-scope reads/writes, network and credential access, dependency-install hooks, resource exhaustion and debug calls. The operating-system boundary must deny access and terminate bounded failures while preserving source/base assets and the prior package.
P02A-T34 | build watching; atomic successful publication; prior working package retained on failure; preview scenario restart initially; compatible incremental reload only at safe boundaries; explicit state migration; snapshot invalidation | Watch valid then broken source revisions and confirm only a complete successful package replaces the prior build. Restart the initial preview explicitly; for later supported reloads test compatibility/migration and snapshot invalidation at declared safe boundaries.
P02A-T35 | generated wall; usable doorway; native door interaction; collectible; full SDK mesh package loader gameplay path; ordinary TypeScript before TSX; bridge before broad editor shell | Author a wall/doorway/collectible in ordinary TypeScript, build actual geometry, load it through C++ and collect through legal native commands. Record source/package/engine identities and prove gameplay runs without a build process or graphical editor.
P02A-T36 | two traversable generated rooms; arched passage; stairs or elevation change; custom procedurally modeled object; NPC and dialogue interaction; one-time reward; generated-only environment and object geometry; separate mixed original or redistributable-model variant | Build and traverse the two-room generated-only scene, using source-generated geometry for every environment/custom object. Independently build a mixed-asset variant and verify licensing/local original references, collision, dialogue and reward behavior in the actual native runtime.
P02A-T37 | agent SDK discovery; agent source authoring; agent build; legal native completion commands; collision and clearance tests; interactions and transitions; reward-once tests; save and restore; leave and re-enter; stepped execution; unthrottled execution; optional native images | Have a generic agent discover the available SDK and build the two-room scene, then complete protected scenario assertions with ordinary native actions. Compare stepped/unlocked results through save/restore and re-entry, retaining legal action traces and optional images as separate evidence.
P02A-T38 | known failing source revision; source-aware diagnosis; source repair; rerun protected tests; targeted follow-up arch revision; preserved unrelated content; preserved stable IDs; delivered source diff; immutable package; optional captures; exact test and replay artifacts | Start with a seeded failing revision, diagnose its native failure, patch only the responsible source and rerun unchanged protected tests. Widen the arch in a follow-up request while preserving unrelated human changes and IDs; deliver reviewable diffs and exact tested package/replay provenance.
P02A-T39 | privileged fixture setup separated from gameplay; actual collision movement; actual eligibility rules; no teleport completion shortcut; no completion-flag mutation; protected acceptance criteria; protected harness configuration; distinct passed failed timed-out and unsupported results | Attempt to pass the scenario via teleportation, flag mutation or acceptance/harness edits and require rejection. Legal failed, unsupported and timed-out runs retain distinct results; a successful run must prove real traversal and actions after clearly recorded fixture setup.
P02A-T40 | prompt-authored town or dungeon; branching quest; optional encounter; failure and retry paths; custom Lua behavior; Phase 7 expansion gate; reference validation; analyzable declarative reachability; actual script and runtime behavior; save and rewind; duplicate-reward prevention; separate presentation review; static checks do not prove arbitrary scripts | Generate and revise a source-authored town/dungeon with branching quest and Lua behavior. Test multiple legal paths, retry/re-entry, optional encounters, save/rewind and reward uniqueness in the native runtime; label static reachability and presentation findings separately from executed script correctness.
P02A-T41 | engine versions; compiler versions; package versions and hashes; seeds; initial state; exact actions; coverage; result assertions; failures; optional images and audio; multiple balance seeds; multiple player policies; simulation versus estimated human time | Audit each verification artifact for reproducible identities and action/assertion data, replay the exact package, and compare results. Run balance samples over recorded seeds/policies and report simulation timings separately from explicit human reading/decision-time estimates.
P03-T01 | compact field-to-battle slice; varied engine exercise; original scenario selection | Select an evidence-backed original checkpoint containing traversal, interactions, event concurrency and a complete battle return; a display-only room does not pass.
P03-T02 | field loading; character rendering; movement; running; jumping; collision; camera rotation; interaction; map transitions | Replay the slice against original observations at terrain and transition boundaries, comparing position/collision outcomes and camera/interaction semantics.
P03-T03 | slice script instructions; dialogue; concurrent activity; waits; triggers; story-state changes | Compare original and native instruction/event traces for normal and rapidly advanced dialogue; required state changes occur once in observed order.
P03-T04 | on-foot action selection; attacks; targeting; damage; enemy behavior; rewards; field return | Use controlled initial battle state and RNG; compare legal actions, outcomes, rewards and post-battle field state to original evidence.
P03-T05 | slice menus; inventory interactions; equipment interactions; ordinary save; ordinary load; keyboard input; controller input | Complete the slice using each device and exercise menu edits plus save/restart/load; recovered state and usable actions match the original contract.
P03-T06 | music sequences; sample playback; sound effects; FMV-to-gameplay transition; independent decoding integration | Play original slice assets locally and compare recorded timing/audio behavior; a full FMV transition restores camera and control with no media drift.
P03-T07 | original reference runs; meaningful state comparison; outcomes comparison; timing comparison; no framebuffer-artifact matching gate; separate optional PS1 fidelity references | Preserve source/input/environment metadata and compare documented gameplay oracles; presentation differences cannot excuse authoritative divergence. Compare Modern gameplay and selected PS1 presentation with separately scoped original oracles.
P03-T08 | field restore; event restore; battle restore; cross-boundary restore; architectural gap closure | Save/restore before, during and after field/event/battle transitions and compare future replay; every discovered state omission is resolved before expansion.
P03-T09 | typed cold-start setup; complete state inspection; traversal jump interaction; dialogue and menu choices; legal complete encounter; save and restore; no OS injection or screenshot interpretation; setup and play provenance separation | Run the frozen original-qualified field/event/battle/reward/menu/save route through a plain protocol client in a device-free process. Require legal action and state transcripts, original comparisons, and no corrective debug mutations during progression.
P03-T10 | exact stepping; semantic readiness; conditional breakpoint; watchpoint; failed assertion artifacts; unthrottled scenario; real-time outcome equivalence; no simplified simulation | Execute first-slice gates for stop boundaries, snapshot/replay and controlled failure; compare real-time/unlocked per-tick state and required work counters in the actual native runtime.
P03-T11 | live human spectator; cadence changes; spectator disconnect; continued agent control; unchanged hashes; no viewer pacing dependency | Attach a viewer mid-slice, slow it, change cadence and disconnect; compare state trace to an image-free run and confirm agent completion at unlocked speed.
P03-T12 | Phase 2A early bridge demonstrated; separate original-game slice evidence; shared native loaders and services; shared movement and interaction rules; shared authoritative state; shared agent API; shared optional renderer; separate original-fidelity and authored-content acceptance | Complete the original slice against original observations and the authored two-room gate against protected source-to-playable assertions. Verify both use the same native services, commands and state model; neither evidence set substitutes for the other before Phase 3 passes.
P04-T01 | terrain/elevation; ladders; traversal; party following; field triggers; camera rules; map transitions; story-dependent field variants | Traverse every inventoried field variant and edge route with automated and manual coverage; compare original position, triggers, companions and transition outcomes.
P04-T02 | complete interpreter; all used instructions; map coverage; scene coverage; branch coverage; instruction-family coverage | Reconcile original script inventory against implemented and exercised instructions; each reachable branch has evidence or an explicit unresolved gap that blocks completion.
P04-T03 | all menus; dialogue; shops; equipment; inventory; party management; ordinary save/load; original-save import; original-save export; declared supported save formats | Exercise all original menu flows and corrupted/unsupported saves; independently round-trip supported original formats without losing state and declare unsupported variants.
P04-T04 | on-foot combos; Deathblows; status effects; enemy AI; scripted encounters; rewards; progression | Cover normal, edge and failure cases for every recovered battle rule and relevant content variant with seeded original/native outcome comparisons.
P04-T05 | Gear battles; Gear resources; Gear equipment; Gear animation; Gear enemy behavior; Gear rules; Gear interfaces | Run representative and exhaustive-rule Gear encounters using original oracles for resources, costs, outcomes and distinct menu/animation behavior.
P04-T06 | world map; transportation; location entry; story-dependent world changes | Traverse each inventoried transport and story variant, entering/exiting locations; compare availability, state and coordinates with original behavior.
P04-T07 | music sequencing; sample playback; sound effects; mixing; FMV demux; FMV decoding; event/media synchronization | Cover the full audio/media catalog with timing and mixing captures; transitions and logical playback state match observed original events.
P04-T08 | original Mono reproduction; original Stereo reproduction; original Wide reproduction; phase and polarity; routing; content path attribution; native signal gate before surround | Match original and native raw two-channel captures for each verified routed path on both discs, with documented timing/level tolerances. Do not validate a decoder against an unvalidated native Wide source.
P04-T09 | every minigame; optional activities; side quests; bosses; ending sequences; failure paths; retry paths | Reconcile per-content and branch inventory with complete original/native runs including failure/retry; unknown or untested content prevents complete-game status.
P04-T10 | disc 1 content; disc 2 content; disc transitions; unified imported-data access; no physical swap in native play | Progress across both verified source profiles and the original disc boundary without physical media switching; compare resulting story and resource state.
P04-T11 | no silent fallbacks; no progression workarounds; unimplemented behavior accounting; unvalidated behavior accounting | Inject unknown opcodes/assets and inspect all fallback sites; diagnostics identify affected coverage and no hidden bypass or dummy result allows a progression pass.
P04-T12 | automated progression checkpoints; manual interaction coverage | Run checkpoint suite and separately record human interactions that automation cannot meaningfully judge; require complete inventory linkage and repeatable setup.
P04-T13 | actions for every subsystem; complete state introspection; typed setup for every subsystem; breakpoints; semantic readiness; world-map setup; battle setup; minigame setup; cutscene setup; progression setup | Reconcile implemented subsystem/catalog coverage with action/query/setup/debug registries and native scenario evidence. Any UI-only or emulator-backlog-limited native subsystem fails completion.
P04-T14 | both-disc headless progression; optional-content headless runs; unlocked speed; reproducible inputs and seeds; bounded assertions; human and agent equivalence; setup coverage distinct from continuous playthrough | Complete original-qualified routes and optional branches through direct commands, with exact hashes and no corrective setup. Report separately cold-launched scenarios and continuous unmodified progression.
P05-T01 | decoded geometry; decoded materials; decoded sprites; decoded textures; decoded camera transforms; native rendering; optional native low-resolution rasterization; no emulator framebuffer | Inspect draw inputs and use independent geometry/asset fixtures; change internal resolution without scaling a captured PS1 framebuffer. Inspect actual native draw inputs and change raster resolution independently of output.
P05-T02 | selectable affine texture distortion; selectable vertex snapping and wobble; selectable low-color quantization; selectable PS1 dithering; selectable low-resolution rasterization; all quirks off in default Modern | Reset config and inspect perspective grids, subpixel motion, gradients and diagonals; Modern passes modern references. Enable each quirk separately and compare its original-qualified fixture with unchanged authoritative state.
P05-T03 | Modern preset; PS1-style preset; Custom preset; affine versus perspective interpolation; projected-vertex quantization; original color precision; dithering control; rasterization-resolution control; recovered primitive-order and depth controls | Inspect effective settings and captures for each preset and individual toggle, including live switching. Applicable quirks match recovered originals while Modern remains the clean-install default.
P05-T04 | correct geometry stage; correct interpolation stage; correct rasterization stage; correct ordering stage; correct color stage; original-capture validation; no substitute screen filters | Use diagnostic meshes/captures isolating each stage and compare source-qualified original fixtures. Blur, noise or scanlines alone cannot satisfy geometric/raster behavior.
P05-T05 | fidelity independent of gameplay time; output resolution independent; aspect independent; culling defaults independent; texture and UI filters independent; widescreen high-refresh fidelity; profiles preserve unrelated settings | Toggle fidelity and profiles across resolution/aspect/refresh/filter/culling combinations; compare state hashes and unaffected configuration values, including migrated settings.
P05-T06 | intentional transparency; masking; palette animation; fog; compositing; selected fidelity behavior | Compare content-specific original effect intent and original-qualified selected fidelity scenes on each backend; audit transparency, masking, palette, fog and compositing ordering independently of authoritative state. Exercise applicable profile switches with original references.
P05-T07 | arbitrary window resolution; arbitrary internal resolution; device limits; independent render scaling; resizing; fullscreen; windowed mode; high-DPI displays | Sweep ordinary and unusual valid dimensions/scales, resize and change display mode; projection/UI remain correct and unsupported sizes produce clear bounded failures.
P05-T08 | arbitrary aspect ratio; correct projection; configurable FOV; UI anchors; safe areas; portrait proportions; text proportions; FMV proportions | Compare 4:3, 16:9, ultrawide, portrait and odd aspect sizes with varied FOV; geometry projection and anchored UI work without stretched portraits/text/video.
P05-T09 | missing-surface audit; incomplete-background audit; out-of-frame staging audit; separate content-repair records | Review wide-framing captures per affected map/scene; classify renderer defects versus missing source content and link each original repair to an intentional-change record.
P05-T10 | arbitrary presentation rate; custom frame caps; uncapped rendering; synchronization options; stable frame pacing | Exercise low, standard, high, fractional and changing refresh/caps plus uncapped and sync modes; measure frame-time distribution and state equivalence on declared hardware.
P05-T11 | camera interpolation; entity interpolation; suitable animation interpolation; authored sprite timing; no mandatory invented animation frames | Compare high-refresh and baseline captures while replaying equal simulation steps; transforms interpolate and source sprite pose cadence remains authored.
P05-T12 | framerate-independent movement; framerate-independent jumping; framerate-independent encounters; framerate-independent battle timing; framerate-independent scripts; framerate-independent minigames; framerate-independent RNG; PS1-fidelity state equivalence | Replay identical inputs/seeds at 30, 60, 120, 144, 240, fractional, variable and uncapped presentation rates; compare tick-aligned authoritative hashes for each domain. Replay with every fidelity toggle and compare all authoritative domains.
P05-T13 | culling default off; independent legacy visibility rejection; independent distance culling; independent frustum culling; independent occlusion culling; independent back-face culling; preserved clipping; preserved depth testing; intentional hidden entities; gameplay activation independent of culling | Reset settings and assert all optional culling modes off; toggle each alone and inspect draw/state deltas; clipping/depth/intentional hiding remain correct and gameplay activation never changes.
P05-T14 | independent world-texture filtering; independent UI filtering; separately evaluated sprite filtering; nearest choice; smoothed choice; no forced UI blur | Change each setting independently in mixed world/UI/sprite scenes; verify serialized settings and pixel output, with sprite-setting inclusion or non-applicability justified explicitly.
P05-T15 | depth ordering; alpha edges; texture seams; palette effects; shader behavior; Vulkan validation; Direct3D 12 validation; Metal validation; Modern backend validation; PS1-style backend validation; Custom backend validation; live profile switching | Run the same reviewed visual fixtures on all backends; compare intended output using predeclared tolerances and inspect boundary artifacts plus device validation errors. Validate each backend/profile combination against the appropriate visual fixtures and unchanged gameplay hashes.
P05-T16 | same renderer for screenshots; offscreen agent observation; optional spectator streams; render skipping preserves simulation; interactive and image-free and streamed equivalence | Render one immutable session state through each output path and replay with rendering disabled. Compare authoritative hashes, capture provenance and drop behavior.
P05-T17 | procedural mod meshes in native renderer; imported mod meshes in same path; supported material subset; visual profiles; optional native captures; source and object attribution; selection and debugging; no browser or external preview authority | Render generated and imported mesh fixtures in each supported native visual profile, inspect material behavior and object/source picking, and retain native captures. External previews cannot supply the native rendering or traversal oracle.
P06-T01 | exploration context; combat context; minigame contexts; game-menu context; app-menu context; editor contexts | Route each action in all active contexts and transitions; irrelevant/covered contexts receive no action and all required controls remain reachable.
P06-T02 | SDL3 gamepad support; hot-plugging; remapping; dead zones; analog movement; rumble; device-appropriate prompts | Use declared device models to connect/disconnect and remap at rest/in play; verify analog/dead-zone response, rumble and prompt changes without stuck actions.
P06-T03 | Windows XInput through SDL3; one physical device identity; no overlapping-provider duplicate actions | Run Windows device-provider combinations and log logical actions; one physical press produces one action and device removal releases state.
P06-T04 | rebindable defaults; WASD movement/navigation; arrow-key movement/navigation; Shift run; run hold/toggle choice; Space jump; Enter confirm; Backspace cancel/back; Q camera left; E camera right; Tab game menu; Esc app menu; V first-person toggle | Reset a clean profile and assert every exact key binding, then invoke each action, rebind it and restart; both movement sets work and Esc never acts as ordinary Cancel.
P06-T05 | all battle action bindings; all minigame action bindings; active-context conflict detection | Reconcile action inventory with keyboard/controller bindings and perform full battle/minigame sessions; duplicate active bindings report conflicts without forbidding harmless cross-context reuse.
P06-T06 | mouse inventory; mouse equipment; mouse shops; mouse battle commands; mouse applicable targeting; mouse save/load; mouse configuration | Complete each menu workflow using pointing and clicking only; meaningful target selection works and accessibility/device fallbacks remain available where targeting is inapplicable.
P06-T07 | real hit-testing; hover feedback; selection; clicking; disabled states; tooltips; direct target activation | Click distant and overlapping UI targets and disabled items; exact hit target receives one action with proper hover/tooltip state, independent of directional navigation distance.
P06-T08 | wheel on every scrollable menu; nested scroll lists; high-resolution wheel; trackpad scrolling | Audit every scroll container and scroll both directions at limits and nested focus; fractional deltas accumulate correctly and events route to the intended container.
P06-T09 | keyboard selection coordination; controller selection coordination; mouse focus coordination; stationary cursor does not steal focus | Switch devices with the cursor resting over another item; selection changes only from intentional input and visible focus matches the active action target.
P06-T10 | Tab toggles permitted game menus; app menu on Esc; Esc independent from skipping; Esc independent from ordinary game-menu Cancel | Press keys through exploration, battles, menus and cutscenes; enforce game-menu eligibility and prove Esc controls the app menu without triggering skip or ordinary Cancel behavior.
P06-T11 | explicit pause behavior; overlay input isolation; text-entry focus; editor focus | Hold gameplay controls while opening overlays or typing; validate pause contract and no leaked actions, stuck keys or hotkeys stealing text input.
P06-T12 | mouse capture; cursor visibility; focus loss; controller disconnection; device switching | Alt-tab, disconnect and switch devices while buttons are held; capture/cursor recover correctly and simulation does not receive stale input.
P06-T13 | physical clients use existing handlers; agents independent of key bindings; agents independent of focus; no virtual devices; semantic toggles; semantic scrolling; semantic selection; equivalent validation for all actions | Change physical bindings/focus while issuing direct actions; agent semantics remain stable. Exercise enabled and disabled physical/semantic selections and compare ticks, costs and state.
P06-T14 | human ownership; agent ownership; explicit handoff; read-only spectator input | Race two controllers and a spectator at handoff boundaries; exactly one owner applies gameplay actions and held inputs release deterministically with logged ticks.
P07-T01 | mod stable IDs; versions; dependencies; compatibility requirements; load order; conflicts; configurable settings | Install synthetic original packages covering valid, missing, cyclic and incompatible metadata; resolve deterministic order and show actionable conflict/version errors.
P07-T02 | map overrides/patches; model overrides/patches; texture overrides/patches; sprite overrides/patches; portrait overrides/patches; UI overrides/patches; dialogue overrides/patches; audio overrides/patches; FMV overrides/patches; encounter overrides/patches; gameplay-table overrides/patches | Independently override and structurally patch each asset family; test precedence and invalid references, preserving immutable imported assets and restoring base behavior when disabled.
P07-T03 | Lua entity API; Lua level API; Lua trigger API; Lua dialogue API; Lua camera API; Lua cutscene API; Lua battle API; Lua minigame API; Lua audio API; Lua UI API | Run documented original Lua examples and API contract/error tests for each domain; examples and versioned documentation agree with exported behavior.
P07-T04 | independent original-bytecode interpreter; Lua behavior extensions; Lua behavior replacement; base content without wholesale rewrite | Run unmodified original event content, then extend/replace one behavior through Lua; compare base behavior with mods disabled and avoid hidden translation requirements.
P07-T05 | replaceable battle mechanics; replaceable minigame mechanics | Change a rule/state transition through an independently authored Lua module; demonstrate outcomes impossible by only changing constants and avoid C++ rebuilds.
P07-T06 | explicit Lua state; versioned Lua state; resumable tasks; snapshot compatibility; rewind compatibility; editor-scrub compatibility; rejected unsupported persistence | Suspend/resume all supported task boundaries through save/rewind/scrub, compare state and reject closures/stacks or patterns outside the persistence contract with repair guidance.
P07-T07 | deterministic event order; deterministic task scheduling; controlled RNG access; simulation-time access; presentation-only scripts; state-changing scripts | Randomize presentation cadence and job timing across repeated seeded runs; compare events/state and deny authoritative writes from presentation-only APIs.
P07-T08 | filesystem restrictions; process restrictions; native-module restrictions; network restrictions; memory budgets; execution budgets; contained script failures | Attempt forbidden access and runaway allocation/execution in original test mods; host remains responsive and errors identify the mod without corrupting authoritative state.
P07-T09 | graphical installation; mod profiles; enable/disable controls; dependency resolution; conflict reporting; safe mode | Manage original packages entirely through UI, recover from broken dependencies and start safe mode with a failing mod installed; base game remains runnable.
P07-T10 | safe-boundary hot reload; script-change snapshot handling; asset-change snapshot handling; state-schema-change snapshot handling; snapshot invalidation/migration | Reload during permitted/forbidden states, then load old snapshots; defer unsafe reload and explicitly migrate or reject incompatibility with no silent divergence.
P07-T11 | independent content-pack interfaces; optional textures; optional audio; optional translations; separately supplied content; no assumed engine-code compatibility | Import documented compatible packs and reject unsupported layouts; clean base install needs none and no third-party game-specific code is adopted.
P07-T12 | original level-event example mod; original cutscene example mod; original battle-behavior example mod; original minigame example mod; no engine recompilation | Install/run each shipped original Lua example through normal package paths, save/restore its modified state and return to unmodified base with mods disabled.
P07-T13 | mod state introspection; mod actions; mod tasks; mod errors; mod setup hooks; breakpoints; snapshots; unlocked tests; published capability and schema extensions | Run all example mods with schemas discovered at runtime; inspect hidden state/tasks, replay/restore and invoke typed setup/actions. Reject an unregistered state/action extension and require actionable errors.
P07-T14 | trusted native debug boundary; untrusted Lua privileges; example mods image-free; example mods streamed | Attempt debug authority escalation from Lua and a spectator, then test authorized host debug. Complete all four example mods in both observation modes with equivalent hashes.
P07-T15 | complete supported Phase 2A SDK and services; schema and catalog discovery; reusable procedural components; scoped edits; source-aware diagnostics; immutable builds; prompt-authored town or dungeon; branching progression; custom Lua behavior | Close every Phase 2A checklist facet for supported content types with actual build/runtime evidence. Produce and revise a town/dungeon using generated geometry, branching events and Lua, retaining source diffs, immutable artifacts and protected native scenario results.
P07-T16 | typed items; characters; Gears; enemy behaviors; encounters; rewards; dialogue; cutscenes; minigame configuration; shared native event and Lua contracts; no second simulation or routine generated C++ | Author one supported definition per domain, type-check/validate its references and run it with the actual corresponding native subsystem. Reject unsupported capabilities; ordinary definitions must not generate or rebuild engine C++ or use alternate gameplay rules.
P07-T17 | standard source project manifest; intent and acceptance specification; TypeScript components; parameter data; optional assets; Lua behavior; scenario tests; creative proposals separated from verified facts; explicit engine capability limits | Build a documented source project from its complete manifest and run its protected scenario tests. Inspect source/provenance and capability declarations; creative proposals and unavailable engine calls remain explicitly distinct from verified original facts and supported operations.
P07-T18 | prompt and retrieval workflow; small specification; blockout; native test; diagnosis and repair; presentation refinement; packaging; targeted follow-up prompt; preserved human changes; stable identities; protected tests; no default whole-project regeneration | Record the full prompt-to-playable loop and a follow-up source revision against a project containing unrelated human edits. Compare diffs and IDs, rerun unchanged native tests against immutable packages, and retain separate presentation review evidence.
P07-T19 | optional editor extensions as trusted tools; external asset generators as trusted tools; prebuilt mod plays without editor plugin execution; prebuilt mod plays without generator execution; generated artwork validated at import; native review of generated artwork | Install and play prebuilt content with its tooling extensions/generators absent or disabled. Attempts to execute tooling during mod load fail; generated artwork must pass format/license/import validation and native review before becoming accepted content.
P08-T01 | versioned snapshot format; game state; original-script task progress; Lua task progress; RNGs; content IDs; active mod versions; reuse foundational debug snapshot service | Save a populated authoritative state and inspect/reload the versioned manifest; every listed domain round-trips and content/mod mismatches are rejected. Verify the player UI and agent restore use one serializer and compatibility policy.
P08-T02 | ordinary-save separation; execution-snapshot separation; save migration; snapshot migration; compatibility policy | Load current, old, future and mismatched save/snapshot fixtures; approved migrations preserve state and unsupported combinations give explicit version diagnostics.
P08-T03 | snapshot slots; quick-save; quick-load; timestamps; thumbnails; undo-load safeguard; atomic writes; corruption checks | Save/load via UI and bindings, undo an accidental load, inject truncation and interruption before/after atomic replacement; previous valid saves survive and metadata remains consistent.
P08-T04 | field-event snapshots; battle snapshots; minigame snapshots; cutscene snapshots; snapshots away from ordinary save locations | Capture at active and transition task points for each domain; restored future replay equals uninterrupted play and no save-safe-only restriction masks missing state.
P08-T05 | configurable fast-forward speed; hold fast forward; toggle fast forward; faster simulation; render-rate independence; independent fully unthrottled agent mode; independent exact stepping; independent bounded run-until; shared core scheduler | Measure simulation ticks per wall second at chosen speeds with fixed render caps; hold/toggle transitions recover normal timing without skipping consequential actions. Confirm player fast-forward preferences do not cap agent-unlocked execution or change tick size.
P08-T06 | time-stretching policy evaluation; mute-above-speed policy evaluation; selected audio-policy threshold; return to normal playback | Document which audio policies are supported, including a reasoned decision on the illustrative stretch/mute choices; change speeds around selected thresholds during music/SFX/FMV playback and verify sync/normal playback recovery. Both example policies are not mandatory.
P08-T07 | periodic rewind snapshots; recorded inputs; recorded events; deterministic rewind replay; duration-or-memory budget decision; chosen budget enforcement; timeline indicator; exposed rewind snapshots | Document the exposed duration or memory budget, rewind through recorded scenarios at its limits, compare replay hashes, enforce the selected budget and inspect accurate available snapshots/history. Both budget controls are not mandatory.
P08-T08 | abandoned-future discard; no duplicate rewards; no replayed persistent writes; no duplicate external side effects | Rewind before a consequential action and resume with changed input; old future vanishes and reward/save/external-effect counters show exactly the intended committed effects.
P08-T09 | logical audio restore; logical FMV restore; decoder rebuild; audio-device rebuild; GPU rebuild | Restore into fresh platform services during active media, compare logical positions and sync, and verify no serialized native handles or stale resources are required.
P08-T10 | Wide decoder history; HRTF convolution tails; resampler delay; portable caches or bounded pre-roll; phase-preserving linked-channel acceleration or mute; load rewind skip and device-switch restoration | Compare continuous and restored offline audio captures from identical logical state, checking linked phase/timing and declared transient/latency tolerances. Require bounded reconstruction before resuming output and no stale cues/clicks.
P08-T11 | rewind map transitions; rewind battles; rewind cutscenes; rewind disc-content transitions; resolved unsupported boundaries | Traverse each boundary backward and forward with original and mod content; compare canonical state and require every unsupported boundary resolved before marking complete.
P08-T12 | agent save and load; agent rewind; branching replay; snapshot inspection; explicit ticks; compatibility errors; isolated saves; image-free state restore; presentation reconstruction on demand | Perform restore/rewind/branch via protocol in fresh processes and private save roots; compare subsequent state hashes and recreated output. Reject incompatible source/schema/mod revisions with no mutation.
P08-T13 | Linux replay equivalence; Windows replay equivalence; macOS replay equivalence; representative mod combinations; explicit incompatible snapshot errors; real-time replay; stepped replay; fixed-speed replay; unthrottled replay; images-off replay; images-on replay | Exchange supported snapshots/replays across declared targets and mod profiles; compare canonical state and reject intentionally incompatible schemas/content instead of accepting drift. Cross modes with representative mod profiles and compare every authoritative tick.
P08-T14 | serialized compiled event-task progress; exact authored-package snapshot identity; save and re-entry during generated doors; quest save and rewind; authored battle save and rewind; Lua task save and rewind; explicit compatible preservation; explicit migration; explicit restart or rejection; no silent definition replacement | Snapshot at authored door/quest/battle/Lua waits, leave/re-enter and rewind with the exact package. Rebuild compatible/incompatible definitions and verify declared preserve/migrate/restart/reject behavior, atomic failures and explicit invalidation instead of silently rebinding saved state.
P09-T01 | player-facing mod research; documented mod behavior; dated adoption evidence; download/community metrics where available; no invented popularity ranking | Record dated primary descriptions and available adoption measurements with source links, scope and uncertainty; an unranked shortlist must be labeled unranked.
P09-T02 | independent option specs; option defaults; option tests; conflict rules; no adopted mod source; no binary-patch foundation | Trace requested player behavior to new specifications/tests and provenance review; inspect dependencies and clean build for absence of borrowed game-specific implementation.
P09-T03 | encounter-rate controls; separate experience multipliers; separate money multipliers; fast text; instant text; optional bug fixes; combat rebalance evaluation; item rebalance evaluation; character rebalance evaluation; other evidence-supported convenience evaluation | Record evidence and accept/defer decisions independently for each option; test selected options against explicit original/control scenarios and verify unrelated settings do not change.
P09-T04 | optional dialogue replacement; optional localization; optional FMV audio; optional subtitles; optional portraits; optional UI replacement; optional textures; separately installable third-party content; base runtime independence | Install and disable each compatible content family through documented interfaces; verify fallback, malformed pack errors and base startup without packs.
P09-T05 | correctness-fix separation; convenience separation; balance separation; translation separation; visual-replacement separation; no mandatory unrelated preset | Toggle categories and individual options independently, restart and compare state/presentation changes; no undocumented bundled modifications appear.
P09-T06 | original-inspected platforming areas; exact map IDs; exact room IDs; documented default list; per-area rationale | Independently observe traversal/encounter interruption for each listed area and link exact source IDs/evidence; no name-based guessed map list is accepted.
P09-T07 | platforming random battles off by default; preserved bosses; preserved mandatory encounters; preserved scripted fights | Reset config and traverse each listed area with controlled encounter conditions; random fights are suppressed while all required/scripted encounters still trigger and progress correctly.
P09-T08 | global encounter override; per-area encounter override; visible explanation of area defaults | Exercise precedence among global and per-area settings after clean install/migration; UI describes the effective default and chosen overrides persist.
P09-T09 | encounter-preparation input loss; safe traversal interruption; jump inputs during preparation; airborne transitions | Replay inputs immediately around encounter scheduling and airborne/map boundaries; accepted jumps/transitions are preserved and no unsafe position/control loss occurs.
P09-T10 | separate reward compensation; no implicit experience inflation; no implicit money inflation | Change encounter rate alone and compare per-encounter experience/money, then explicitly enable compensation and verify only the documented reward behavior changes.
P09-T11 | reveal speed independent of event timing; reveal speed independent of completion; waits with fast/instant text; auto-close dialogue; concurrent actor movement; rapidly advanced conversations | Run original and modified text speeds over event edge cases; consequential state/order remains equivalent and dialogue does not skip waits or strand actors.
P09-T12 | cutscene pause; visible skip action; configurable hold-to-skip; Esc app menu independent of skip | Pause/skip using all input devices and vary hold duration; short accidental presses do not skip and Esc always follows the app-menu contract.
P09-T13 | presentation bypass; preserved story flags; preserved party changes; preserved inventory; preserved rewards; preserved transitions; other consequential actions | Compare watched and semantic-skipped runs from identical checkpoints; canonical consequential state is equal and bypassed presentation has no missing/extra effects.
P09-T14 | stop at meaningful choices; stop at mandatory gameplay; stop at battles; no silent choice selection | Skip toward each boundary and require visible player control/choice; branch outcome cannot be chosen implicitly and subsequent watched/skipped state matches the selected branch.
P09-T15 | complex-scene completion handlers; validated safe skip segments; Lua skip hooks; Lua completion hooks; cutscene-editor skip/completion hooks | Author/test a complex scene through native and Lua/editor paths; prove segment entry/exit preconditions and exactly-once consequences on watched, skipped and resumed paths.
P09-T16 | consequential changes exactly once; correct camera return; correct location return; correct actor return; correct player-control return | Repeated skip, reload and rewind around completion; count changes and compare final location/actors/camera/control to the equivalent watched path.
P09-T17 | every supported watched/skip scene path; scene entry after load; scene entry after rewind | Reconcile scene/branch inventory with paired watched/skipped runs including restored entry points; unsupported paths stay explicit and block skip-completion claims.
P09-T18 | agent option access; dialogue choices; explicit skip command; natural unlocked completion; semantic skipping; no implicit headless skips; no automatic choices; no debug substitute for consequences | Pair watched-unlocked and explicitly skipped traces for each branch; compare consequential state, stop at required choices/battles and confirm no hidden debug edits or auto-selection.
P10-T01 | playable first-person camera; mouse look; controller look; camera-relative movement; configurable FOV; configurable sensitivity; configurable inversion | Traverse original field scenarios from first person using both devices and setting extremes; camera/movement controls remain usable and settings persist.
P10-T02 | first-person running; first-person jumping; first-person collisions; first-person interaction; first-person traversal | Replay equivalent third/first-person routes and interactions against recovered simulation rules; changing camera mode does not detach or bypass gameplay collision/state.
P10-T03 | on-foot exploration; Gear exploration; battle camera transition; minigame camera transition; authored cinematic framing | Switch modes and enter/exit each domain; follow an explicit transition contract and restore usable exploration camera without stale input capture.
P10-T04 | controlled-character hiding/adaptation; sprite orientation; targeting; player near-plane clipping prevention; scenery near-plane clipping prevention | Inspect close geometry and actor views while moving/targeting through edge cases; representation and near plane remain coherent without seeing through the controlled model or walls.
P10-T05 | scripted camera ownership; selected-mode restoration; orientation restoration | Enter scenes from both camera modes/orientations, watch/skip/restore them and verify authored framing then correct user camera handoff.
P10-T06 | first-person missing-surface audit; wide-FOV incomplete-scenery audit; original content overrides; no culling-as-geometry repair assumption | Inspect all affected views, link gaps to content records and validate independently authored repairs; visibility toggles cannot be counted as manufacturing missing surfaces.
P10-T07 | original FMV decode quality; correct proportions; correct color; correct timing; audio synchronization | Inspect original-mode videos against independently decoded source measurements for dimensions, color/timing and sync using declared tolerances and full transition coverage.
P10-T08 | reviewed deblocking; reviewed denoising; upscaling; derived-cache or replacement strategy; chosen enhanced-media storage path | Compare original/enhanced per-video samples with documented review and provenance; document cache/replacement strategy, regenerate/invalidate derived media by source/settings where used, and retain base playback without enhancements. Both storage alternatives are not mandatory.
P10-T09 | explicit Original setting; explicit Enhanced setting; per-video original fallback; enhanced media optional | Select both modes after clean install, delete/malform one enhanced video and verify original fallback for that video with working playback and transitions.
P10-T10 | independent scaling; independent sharpening; independent motion interpolation; no synthetic FMV frames forced by gameplay refresh | Toggle each enhancement alone and change game framerate; original video frame cadence remains available and unselected transforms do not run.
P10-T11 | face review; linework review; subtitle review; motion review; compression-artifact review; skip behavior; seek behavior; gameplay return; both FMV modes | Review every media entry using the visual checklist and seek/skip transitions in Original and Enhanced modes; record artifacts and per-video fallback decisions.
P10-T12 | completed original Wide analysis; validated native Wide input; intended spatial reference; signal relationships; routing; unresolved ambiguities; no assumed Dolby or discrete-channel format | Require reviewed Phase 1 findings and Phase 4 native signal comparisons before decoder acceptance. Any unresolved claim about spatial encoding remains blocked rather than inferred from the Wide label.
P10-T13 | independent appropriate decoder; evidence-supported matrix use; phase-expansion virtual playback alternative; no invented anti-phase rear routing | Select architecture from measured signal hypotheses and validate the chosen reconstruction against matched original captures and synthetic polarity/phase cases. Record why rejected hypotheses fail and preserve unsupported ambiguity.
P10-T14 | justified spatial components only; left and right placement; centered content; ambience; verified surround cues; no invented rear height LFE or object locations | Feed isolated channel/phase/center fixtures and original mixes; compare routing to the established spatial reference, documenting leakage/tolerance and excluding unsupported channels.
P10-T15 | HRTF binaural rendering; exactly two headphone channels; no simple stereo downmix; independent infrastructure selection; licensed HRTF data; reference profile; optional calibrated choices | Audit dependency/data provenance and validate direction/phase/latency with reference fixtures and listening comparisons on ordinary stereo headphones. Inspect endpoint channel count and profile selection.
P10-T16 | optional Headphones Wide surround mode; native Wide processed once; original Mono retained; original Stereo retained; undecoded Wide retained; previous original mode restored on disable | Toggle every original/output combination and inspect signal taps and effective state; count processing stages and verify exact prior-mode restoration with no mandatory content pack.
P10-T17 | verified music routing; effects routing; reverb routing; FMV routing; replacement-audio routing; preserved phase and timing; no double virtualization; no assumed Wide in all stereo assets | Use tagged original and replacement signals for every path; bypass already spatialized/unencoded sources according to declared provenance and compare raw/intermediate/output taps.
P10-T18 | faithful original Wide field; no arbitrary emitter repositioning; no first-person field rotation; no unsupported rear or room effects | Compare third/first-person output from identical original mix and reference settings; camera changes must not invent scene-relative cues or undocumented reverberation.
P10-T19 | headroom; mix balance; transients; linked-channel gain; latency compensation; click-free switching; level-matched Stereo A/B; undecoded Wide A/B; explicit external-virtualization handling | Measure peak/gain/delay and switching transients with synthetic and local original captures; conduct level-matched listening comparisons. Expose external virtualization assumptions without silently stacking it.
P10-T20 | original versus native Wide tests; separate decoder tests; separate binaural tests; synthetic phase polarity and panning fixtures; local captures on both discs; listening against spatial reference; generic widener rejected | Report independent input, reconstruction and HRTF oracles, signal metrics and listening results with source identities. A passing final stereo output cannot conceal failure of an earlier signal stage.
P10-T21 | state restoration; acceleration; sample-rate changes; device reconnects; Linux two-channel output; Windows two-channel output; macOS two-channel output; no external processor | Run Phase 8 transition fixtures on ordinary two-channel devices per platform and compare offline/live signal behavior within declared tolerances, with profile/device identities.
P10-T22 | agent camera mode and orientation; media timeline; sound mode; processing parameters; diagnostic captures; first-person spectator; original and enhanced video; all visual profiles | Discover/query/change each setting through the same native services and inspect optional captures; replay without images and require equal authoritative state.
P10-T23 | logical media completion with output disabled; logical cutscene synchronization; offline audio capture; offline video capture; offline DSP capture; no device or wall-playback requirement; rebuild output history | Run media/event scenarios to the same logical endpoint with output off and offline/live captures; verify completion events and bounded presentation pre-roll before output reattachment.
P11-T01 | shared editor shell; project management; asset browser; inspectors; console; undo; redo; autosave; validation | Create/open/save/recover a project, browse/inspect assets and edit/undo/redo; errors surface in UI and autosave survives interruption without corrupting project data.
P11-T02 | shared renderer; shared importers; shared simulation; shared state system; shared Lua APIs; exact runtime previews | Run identical project/checkpoint in editor preview and normal build; compare authoritative state and intended rendering using the same runtime services.
P11-T03 | geometry editing; geometry import; geometry export; object placement; collision editing; spawn editing; map connections; trigger volumes; encounter regions; camera editing | Author each capability graphically, undo/redo, save/reopen and export; play the resulting mod in a normal build and validate references and geometry/collision behavior.
P11-T04 | collision visualization; event-volume visualization; navigation constraints; visibility rules; missing-geometry visualization | Seed known original synthetic inspection problems and use overlays to locate them; overlays match runtime data and do not change simulation behavior.
P11-T05 | cutscene timeline; branching event graph; actor tracks; movement tracks; camera tracks; dialogue tracks; audio tracks; FMV tracks; story-change tracks; interaction tracks; battle tracks | Build a branching scene using every track type and play each path in editor/normal runtime; timing, task order and consequences match authored data.
P11-T06 | recovered original-script import; opaque construct preservation; unconverted construct preservation; explicit flags for unknown meaning | Import/edit/export a script containing known and unknown constructs; untouched opaque bytes/structure survive losslessly and UI flags unknown semantics instead of replacing them.
P11-T07 | Lua diagnostics; API completion; breakpoints; variable inspection; execution stepping | Edit erroneous and valid Lua, inspect completions, pause at breakpoints and step tasks while watching variables; tooling references the actual versioned API/state.
P11-T08 | visual-graph/handwritten-Lua boundary; timeline/handwritten-Lua boundary; custom Lua node preservation; regeneration safety | Round-trip visual edits and regeneration around custom handwritten nodes; preserve code and declared ordering without destructive transformations.
P11-T09 | snapshot-based scrubbing; deterministic replay preview; no duplicate story consequences; no permanent preview consequences | Scrub repeatedly across rewards/flags/transitions, branch preview and close/reopen; canonical preview state matches replay and project/game persistent state receives no unintended writes.
P11-T10 | graphical skip-segment authoring; graphical completion-handler authoring; mandatory-choice authoring; return-to-gameplay authoring; skip validation | Author watched/skipped paths in UI and validate required boundaries and exactly-once changes; generated package preserves state and restores control in normal runtime.
P11-T11 | version-control-friendly projects; native mod export; immutable original asset store | Review textual diffs after a local edit, reopen/export package and play in a normal build; imported-store hashes remain unchanged and project metadata is portable.
P11-T12 | shared editor play services; shared debug services; shared inspection; shared go-anywhere; agent launch edited levels; agent test cutscenes; optional live images and state | Create/export edited content, launch through the protocol without editor UI, and compare normal/editor previews. Confirm identical command/query/debug handlers and scene results.
P11-T13 | direct TS and TSX component opening; parameter schema inspection; generated geometry and instance source maps; shared revisioned commands; shared build services; persistent source documents; persistent history outside disposable UI components | Open and edit a source project through graphical panels and agent commands, rebuild and select generated objects via source maps. Destroy/recreate panels and verify source/history survive; concurrent edits obey the same revisions and validation.
P11-T14 | explicit editable source boundaries; exposed parameters; supported declarative graphs; reviewable code-driven source patches; preserved handwritten algorithms; preserved expressions; preserved Lua nodes; preserved opaque original scripts; no arbitrary scene-to-source regeneration | Edit parameters/graphs around custom code and opaque script fixtures, reviewing any proposed source patch. Compare untouched source bytes and semantic identities after build/reopen; unsupported source round-tripping must be rejected explicitly.
P11-T15 | selection entity and component IDs; selected source revision; selected properties; graph nodes; diagnostics; optional viewport captures; targeted prompt edits; reviewable diffs; undo; preserved unrelated rooms; stale human-edit conflict handling | Supply selection context to an agent, make an intervening human edit, then request a targeted change. Reject stale writes or resolve them as reviewable transactions, preserve unrelated content and verify undo restores the complete prior source revision.
P11-T16 | procedural geometry inspector parameters; material parameters; collision parameters; connection anchors; source-correlated errors; affected-content rebuild; native renderer preview; native traversal tests; native event tests; external previews supplemental only | Modify geometry/material/collision parameters and anchors through inspectors, rebuild affected outputs and exercise traversal/events in the native runtime. Source errors point to editable parameters; a successful external preview cannot override failed native tests.
P11-T17 | documented inspector extensions; generator extensions; visualization extensions; matching headless content operations; separately enabled trusted tooling; no automatic extension execution on gameplay load; QML or UI toolkit independent from TypeScript source; UI toolkit independent from native runtime | Exercise each enabled tooling extension through its documented services and corresponding headless operations, then play exported content with all extensions/UI absent. Verify trust enablement and unchanged source/runtime contracts across UI clients.
P12-T01 | character editor; Gear editor; enemy editor; equipment editor; item editor; ability editor; status-effect editor; progression editor; encounter editor; reward editor | Create/modify each data domain graphically, validate links and save/export/reopen; normal runtime reflects authored changes without rebuilding C++.
P12-T02 | action availability; action costs; turn scheduling; targeting; damage; defense; combos; Deathblows; Gear rules; victory conditions; defeat conditions | Author a rule change for each boundary and run seeded isolated battles; legal actions, resources and outcomes follow the new rules and persist through restore.
P12-T03 | replaceable Lua battle-rule modules; mechanics beyond immutable callbacks | Replace action/turn/outcome logic through tools and Lua, demonstrate a substantially different ruleset and verify no hidden fixed C++ rule overrides it.
P12-T04 | battle animation editing; battle effects editing; battle camera editing; battle interface editing; immediate runtime preview | Edit each presentation channel and preview/replay the encounter; normal exported build reproduces the preview while authoritative battle rules stay consistent.
P12-T05 | minigame arena/layout tools; entity tools; input-action tools; rule tools; scoring tools; timing tools; opponent-behavior tools; win-condition tools; loss-condition tools | Redesign each minigame domain graphically and verify deterministic play plus failure/retry; editor exposes behavior changes rather than assets/difficulty alone.
P12-T06 | Lua minigame state machines; Lua minigame runtime behavior | Replace transitions and runtime rules through Lua, snapshot/restore midway and demonstrate materially changed behavior without C++ edits.
P12-T07 | isolated battle sessions; isolated minigame sessions; configurable starting state; deterministic seeds; pause inspection; step inspection; repeatable automated runs | Launch repeated isolated tests from saved test definitions with varied seeds/state; pause/step and compare exact replay hashes and diagnostics.
P12-T08 | reference validation; resource validation; script-failure validation; dependency validation; persistence validation; compatibility validation; pre-export validation | Attempt export with each broken domain and require precise errors; valid packages pass round-trip/replay checks and declare compatible targets/content/mod versions.
P12-T09 | original changed-battle example project; original redesigned-minigame example project; tools-and-Lua-only workflow; no C++ rebuild | Follow published example projects from editor creation to packaged play and restore; reviewers verify substantial mechanics changes and independent authorship.
P12-T10 | native battle go-anywhere; minigame go-anywhere; direct commands; full creator-defined state; unthrottled tests; versioned mod debug schema; optional human images | Author changed battle/minigame rules and launch, play, inspect and debug them through discovered extensions without C++ changes. Compare deterministic image-free and streamed runs at unlocked speed.
P12-T11 | agent-authored enemy encounter or minigame; procedural arena geometry; typed TypeScript definitions; custom Lua mechanics; exposed editor parameters; native scenario tests; multiple balance seeds; multiple player policies; legal-action evidence; persistence evidence; exact source revision evidence | Create and revise an enemy/encounter or minigame from a prompt using generated arena geometry and Lua. Run legal native scenarios across recorded seeds/policies, save/restore state and editor parameter revisions, retaining exact immutable package/source and balance results.
P13-T01 | Arch Linux Vulkan priority; Wayland; Hyprland; X11; fractional scaling; mixed-DPI displays; ultrawide framing; fullscreen changes; focus loss; controller reconnect | Run declared Arch hardware/session matrix and capture display/input/state behavior under changes; record host, compositor and driver with each result.
P13-T02 | Windows Direct3D 12; SDL3 input on Windows; XInput device behavior | Install/run Windows package and full device/backend scenarios on declared Windows targets; one physical controller yields one logical action.
P13-T03 | macOS Metal; explicit processor targets; explicit OS targets; application lifecycle; macOS input; macOS display behavior | Declare supported OS/CPU combinations before release and run launch/quit/suspend/resume, device and display cases natively on each; untested targets are not advertised supported.
P13-T04 | shader compilation; rendering correctness; resource lifetime; frame pacing; authoritative state across platforms/backends | Run common shader/visual/replay suites and backend validation layers; enforce documented visual/timing thresholds and identical canonical state where compatibility is claimed.
P13-T05 | complete default playthrough; complete original-gameplay playthrough; optional-content coverage; representative mod combinations; independent gameplay and visual profiles; Modern visuals; PS1-style visuals; Custom visuals; original sound modes; independent Wide headphone option | Reconcile both-disc playthrough and optional-content coverage under default, original-gameplay and representative mod settings. Compare equivalent authoritative state across independently chosen visual and sound modes, using separate fidelity signal/capture oracles. No gameplay preset may silently force a presentation or audio profile.
P13-T06 | no display server; no GPU; no physical input; no audio device; same headless protocol on Linux; on Windows; on macOS; images explicitly renderer-dependent | Run native installed packages in environments denying the listed devices/services; play the slice and supported complete scenarios using pipes. Record native platform evidence separately from rendering qualification.
P13-T07 | agent playthrough CI; go-anywhere CI; complete state and assertions; semantic readiness; deterministic replay; unlocked execution; evolving emulator parity; native extensions tested | Reconcile CI execution manifests with every parity row, implemented subsystem and progression branch. Fail for missing adapters, hidden UI dependencies, unsupported setup or absent native evidence.
P13-T08 | interactive equivalence; image-free equivalence; offscreen equivalence; streamed equivalence; real-time equivalence; stepped equivalence; fixed-speed equivalence; unlocked equivalence; supported mods; presentation differences classified | Cross the mode matrix with identical canonical action traces, compare every authoritative hash/event and separately report expected presentation and performance differences.
P13-T09 | protocol versions; capability discovery; invalid requests; stale snapshots; cancellation; concurrent inspectors; control handoff; debug permissions; disconnect and reconnect; bounded queries; image backpressure; complete failure artifacts | Execute the versioned native acceptance catalog against installed packages using adversarial independent clients. Require terminal structured outcomes and unchanged state for rejected mutations; no synthetic specification test counts as runtime evidence.
P13-T10 | Wide reconstruction to binaural path; exactly two-channel headphones; signal validation; listening comparisons; sample-rate changes; latency changes; restoration; no multichannel endpoint; no external processor | Validate input/decoder/HRTF stages and restored full output on ordinary headphones across platforms using established original references and level-matched listening; record exact routing and device format.
P13-T11 | arbitrary-framerate stress; fast-forward stress; repeated rewind stress; repeated load stress; skip stress; device-change stress; malformed-asset stress; malformed-mod stress; interrupted-write stress | Run bounded documented stress schedules with sanitizers/validation and resource measurements; no corruption, divergent replay or unrecoverable persisted data is accepted.
P13-T12 | CPU performance; GPU performance; memory performance; snapshot-storage performance; loading performance; editor performance; documented hardware; regression budgets; headless ticks per second; separate rendering and streaming costs; separate Wide and HRTF costs; no hidden real-time cap | Measure repeatable workloads on named hardware with versioned thresholds; compare baseline distributions and resolve or explicitly approve every release regression. Record wall/simulation work and wait attribution in unlocked benchmarks on named hardware.
P13-T13 | Arch PKGBUILD; Windows releases; macOS application bundle; packaged editor suite; packaged native headless entry point; documented agent client; shipped debug features; explicit access controls | Install/uninstall each clean package and launch both game and every editor; verify dependencies, licenses, filesystem placement and clean startup. Use installed packages to run discovery, gameplay, debug and spectator role tests.
P13-T14 | first-run disc import; hash validation; revision validation; configuration migration; controller setup; understandable failures; noninteractive agent startup; noninteractive import; noninteractive configuration; no GUI dialogs in headless flow | Exercise first run with supported/unsupported/corrupt/missing media, old config and devices; explain failures and recover without partial trusted stores or lost settings. Perform first run through pipes with invalid/missing data and verify structured recovery errors.
P13-T15 | clean culling-off default; clean platforming random-battles-off default; clean Tab game menu; clean Esc app menu; clean independent texture/UI filters; clean selectable original FMVs; Modern clean default; PS1 options present but off; optional Wide headphones; original sound modes retained; image stream off by default; local control by default; explicit headless speed selection | Remove all user settings/mods and inspect effective defaults in every package; invoke each default behavior and verify migration never silently bundles unrelated changes. Inspect effective clean and migrated settings through the native API and exercise independent toggles.
P13-T16 | clean independent build; clean independent base runtime; no other Xenogears project; no preexisting mod; no third-party game-specific code | Build/install/run in a documented environment containing only reviewed infrastructure and user original data; audit source/dependency provenance and run base progression without mods.
P13-T17 | no source images in artifacts; no executable dumps; no extracted assets; no private saves; no unintended development files | Enumerate installed and release/archive contents against explicit allowlists and license/provenance records; fail packaging on any private or unreviewed payload.
P13-T18 | developer setup docs; independent research notes; decompilation notes; player docs; Lua API docs; editor tutorials; content-pack guidance; compatibility policies; versioning policies; agent protocol and schemas; direct-input examples; full debug and go-anywhere guide; spectator setup; speed semantics; reproducible native scenarios; PS1 visual options; Wide and HRTF evidence and provenance | Follow documentation from clean installations and original authored projects; verify every documented command/API against the shipped version and link policies to actual diagnostics. Follow protocol examples with an independent plain client against the installed package, not a documentation mock.
P13-T19 | distributed TypeScript SDK; separately packaged pinned Node and build dependencies; geometry generators; schema and catalog tools; headless content CLI; separate authoring and shipping runtime requirements; prebuilt mods without Node; prebuilt mods without Qt or QML; prebuilt mods without browser; prebuilt mods without external modeling tools; all supported native platforms | Install authoring tools and build documented examples on supported toolchains, then install only the prebuilt native game/mod on clean supported platforms with authoring dependencies absent. Complete legal gameplay tests and inspect dynamic/runtime dependencies and package provenance.
P13-T20 | public source-to-native CI tests; collision and material tests; schema-parity tests; protected scenario tests; malformed-package tests; generator isolation tests; reload and migration tests; content-conflict tests; original redistributable fixtures; exact package hashes; supported-toolchain reproducibility results; local original data excluded from public artifacts | Run the complete authoring CI suite from public source-only fixtures on supported targets. Inspect protected harness integrity, generator denial cases, malformed load rollback, migration/conflict behavior and artifact hashes; source archives/logs must contain no locally supplied game data.
P13-T21 | generated-only geometry examples; mixed imported-asset examples; declarative event examples; custom Lua examples; targeted revision examples; source-aware repair examples; graphical parameter-edit examples; build/runtime architecture diagrams; exact CLI and RPC contracts; extension trust rules; capability and unsupported-feature catalog; original example provenance | Reproduce every published example and revision using only documented SDK/CLI/RPC contracts and permitted assets. Compare tested package identities and native assertions, inspect source diagrams and trust rules, and reject any documented capability without implementation/evidence.
P13-T22 | every matrix row passes; complete runtime beyond story completion; complete creator toolkit; all declared platform targets; complete agent-native runtime; complete source-authoring pipeline | Reconcile facet-level executed evidence with all source tasks, defaults, platform cells and release gates; missing/not-run/failed/blocked cells prevent 1.0 approval. Require headless, parity, protocol, original-slice, authored early-bridge, expanded SDK and source-preserving editor gates in addition to story completion.
